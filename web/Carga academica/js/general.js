/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/*
 $(document).ready (function(){
       $("#error").hide();
           
});
*/

/*
$('#codigo').change(function(){
         $('#form')[0].reset();
})

*/


/**
 * este metodo me permite validar que la cantidad de creditos corresponda con el numero de 
 * horas digitadas y bloque el boton de registro si no conindice 
 * @param {type} param1
 * @param {type} param2
 */
/*
$(".horasD").bind('keyup mouseup', function () {
     // initialize the sum (total price) to zero
      $("#registrar").prop('disabled', false);
    var sum = 0;
    $('.horasD').each(function() {
        sum += Number($(this).val());
    });
    var cre=parseInt(obtenerCredito());
    
    if(sum>cre){
        $("#registrar").prop('disabled', true);
           $("#error").show();
            $("#error").fadeTo(4000, 500).slideUp(500, function () {
                $("#error").slideUp(500);
            });

    }
    
    $('#semanal').val(sum);
    $('#semestral').val(sum*16);
             
});
  */ 
 
 /*
$(".horasE").bind('keyup mouseup', function () {   
     // initialize the sum (total price) to zero
    var sum = 0;
    $('.horasE').each(function() {
        sum += Number($(this).val());
    });
    $('#horaSemestre').val(sum);
     
});
*/
/*
function obtenerCredito() {
   var d = document.getElementById("codigo").value;
   var cre= d.split("") ;
   return cre[cre.length-1];
}
*/